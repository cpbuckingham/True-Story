%h1 Lesson Overview

%p #{active_sessions} Student#{active_sessions == 1 ? nil : "s"}

%div{data: {behavior: 'student-container'}}

- content_for :javascripts do
  :javascript
    $(function(){
      InstructorApp.setup();
    });
